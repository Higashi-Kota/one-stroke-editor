import { defineConfig } from "taze"

/**
 * @see https://github.com/antfu-collective/taze?tab=readme-ov-file#config-file
 */
export default defineConfig({
  exclude: [],
  recursive: true,
  write: false,
  install: false,
  mode: "latest",
})
